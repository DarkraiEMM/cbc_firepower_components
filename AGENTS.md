# Minecraft 炮台模组协作规则

回答与实现时避免过分夸赞。用户与助手的判断都可能有误；优先检查现有代码、资源和实际工具状态，再给出结论。

## 通用美术规则

涉及 Minecraft 模型、纹理、UV、动画、Blockbench、模型 JSON、方块状态、碰撞或资源导出时，使用全局 `minecraft-mod-lowpoly-assets` 技能。通用的原版体素风格、低模高纹理、Blockbench MCP 检查点、分阶段截图和导出验证规则由该技能统一维护；本文件只补充当前炮台项目的特例。

## 炮台模型层级

自动炮台默认采用以下逻辑层级，并根据实际实现调整：

```text
root
├─ base                 固定底座
├─ yaw                  水平旋转组件
│  └─ pitch             俯仰旋转组件
│     ├─ head           武器主体
│     ├─ barrels        枪管或发射组件
│     └─ muzzle         枪口、光束或粒子锚点
└─ optional_components  弹药、能源、天线和装饰模块
```

- 枢轴必须放在真实机械转轴上，不能只靠视觉接近。
- 共用炮座与不同炮头应保持一致的安装面、原点、朝向和尺寸接口。
- 机枪、加农炮、导弹、激光、压制和电浆炮头应共享命名和骨骼约定，避免每种炮头单独适配渲染代码。
- 会后坐的部件与固定外壳分组；发光、枪口焰和光束起点使用独立锚点或明确的命名部件。

## 项目资源与兼容

- 修改前匹配项目现有的资源命名、目录、模型父级、纹理路径、渲染类型和数据生成方式。
- 同时存在 Forge 1.20.1 与 NeoForge 1.21.1 资源时，明确主版本与需要同步的版本，不默认为两个版本机械覆盖。
- 参考外部模型时只借鉴轮廓、比例、分组和纹理方法；除非许可证允许，不直接复制模型或纹理。
- 导出后检查模型引用、纹理引用、粒子纹理、透明通道、物品显示变换和动画枢轴。
- 运行与改动范围相称的最小验证；未运行的构建、数据生成或游戏内检查需要明确说明。
